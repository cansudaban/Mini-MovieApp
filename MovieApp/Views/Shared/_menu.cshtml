@model MovieCategoryModel

<div class="list-group">
    @foreach (var item in Model.Categories)
    {
        <a href="/Home/Index/@item.Id" class="list-group-item list-group-item-action">@item.Name</a>
    }
</div>